{
  "name": "deployments",
  "version": "0.1.0",
  "bin": {
    "deployments": "bin/index.js"
  },
  "scripts": {
    "deploy": "zx ./scripts/deploy.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-cdk-lib": "2.77.0",
    "constructs": "^10.0.0"
  }
}
